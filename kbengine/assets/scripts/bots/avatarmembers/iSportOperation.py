# -*- coding:utf-8 -*-
from KBEDebug import *


class iSportOperation:
    def gotWeeklySportRank(self, weekly_record_list):
        # DEBUG_MSG("%s[%i] %s" % (self.__class__.__name__, self.id, sys._getframe().f_code.co_name))
        return

    def pushDailySportRank(self, state, weekly_record_list, ):
        # DEBUG_MSG("%s[%i] %s" % (self.__class__.__name__, self.id, sys._getframe().f_code.co_name))
        return

    def pushSportTimesList(self, free_sport_list):
        # DEBUG_MSG("%s[%i] %s" % (self.__class__.__name__, self.id, sys._getframe().f_code.co_name))
        return

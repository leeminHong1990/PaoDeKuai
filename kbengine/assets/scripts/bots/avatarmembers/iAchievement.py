# -*- coding: utf-8 -*-

import KBEngine
from KBEDebug import *

class iAchievement:
	""" 签到成就相关 """

	def __init__(self):
		pass

	def pushSignInNum(self, signInNum):
		# DEBUG_MSG("%s[%i] %s" % (self.__class__.__name__, self.id, sys._getframe().f_code.co_name))
		return

	def signInFailed(self):
		# DEBUG_MSG("%s[%i] %s" % (self.__class__.__name__, self.id, sys._getframe().f_code.co_name))
		return

	def pushSignInfo(self, info):
		# DEBUG_MSG("%s[%i] %s" % (self.__class__.__name__, self.id, sys._getframe().f_code.co_name))
		return
